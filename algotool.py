####################################################################
#
#   Application : MaxArm ESP32 Tools
#   
#   Add : Praise the Lord, not me
#
#   Programmer : Hunter M.
#
#   Use Of Code : This code is to write Software to control ESP32 MaxArm Robot, intended for, New Prairie High School
#
####################################################################

"""
This file is used for creating algorithms in this 3D editor
"""

class __Algorithim__:
    
    #stores 2 hash values in tiny string table of 2
    def InsertHash(char, char_K):
        hashTblAssert = str[2]
        for i in range(0):
            char = hashTblAssert[i][0]
            char_K = hashTblAssert[i][1]
     
    #just compares values then id's them       
    def Sort(_item, _item2):
        if _item > _item2:
            id(_item)
        if _item2 > _item:
            id(_item2)
        if _item == _item2:
            print('sorting error, values are the same')        
            
            #============
    # reserve()
    def reserve(num):
     for i in range(0):
        setNum = set()
        setNum.add(num)
        